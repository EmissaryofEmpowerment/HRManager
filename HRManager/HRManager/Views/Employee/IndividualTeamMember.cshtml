@model HRManager.Models.Employee
@{
    ViewData["Title"] = "Individual Team Member";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>IndividualTeamMember</h1>
<form>
    <fieldset>@Model.FirstName @Model.LastName</fieldset>
    <table>
        <tr>
        <thead>
            <tr>
                <th>Employee ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Address</th>
                <th>Email</th>
                <th>Phone Number</th>
                <th>Position</th>
                <th>Department</th>
                <th>Start Date</th>
                <th>End Date</th>
                <th>Employment Status</th>
                <th>Shift</th>
                <th>Direct Supervisor</th>
                <th>Favorite Color</th>
                <th>Permission Level</th>
                <th>User Name</th>
                <th>Password</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td style="text-align:center">@Model.EmployeeId</td>
                <td style="text-align:center">@Model.FirstName</td>
                <td style="text-align:center">@Model.LastName</td>
                <td style="text-align:center">@Model.Address</td>
                <td style="text-align:center">@Model.Email</td>
                <td style="text-align:center">@Model.PhoneNumber</td>
                <td style="text-align:center">@Model.Position</td>
                <td style="text-align:center">@Model.Department</td>
                <td style="text-align:center">@Model.StartDate</td>
                <td style="text-align:center">@Model.EndDate</td>
                <td style="text-align:center">@Model.EmploymentStatus</td>
                <td style="text-align:center">@Model.Shift</td>
                <td style="text-align:center">@Model.Manager</td>
                <td style="text-align:center">@Model.FavoriteColor</td>
                <td style="text-align:center">@Model.PermissionLevel</td>
                @*I need a way to make sure that the only
        users that can see the Employee's UserName
        and Password is that individual Employee
        plus people who have a high enough permisson level*@
                <td>@Model.UserName</td>
                <td>@Model.PassWord</td>
            </tr>
        </tbody>
    </table>
    <button asp-area="" asp-controller="Employee"
            asp-action="CreateRecord"
            asp-route-id="@Model.EmployeeId"
            style="background-color:#EEE; color:midnightblue;">
        Add New Team Member
    </button>
    @*<button>Edit This Record</button>*@
    <button style="background-color:#EEE;"
            asp-route-id="@Model.EmployeeId"
            asp-action="Delete"
            onclick="return confirm('Are you sure you want to delete this record?')">
        Delete This Record
    </button>
</form>

